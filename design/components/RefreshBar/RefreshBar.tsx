import {View} from 'react-native';

export const RefreshBar = () => {
  return (
    <View
      style={{
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: 'white',
        borderWidth: 1,
        borderColor: 'lightgray',
        borderRadius: 8,
        paddingLeft: 12,
      }}
    />
  );
};
